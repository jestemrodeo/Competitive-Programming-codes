#include<cstdio>
#include<iostream>
#include<deque>
#include<vector>
#include<queue>
#include<set>


using namespace std;

int main(){
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	deque<int> d;
	d.push_back(8);
	d.push_back(12);
	d.push_front(10);
	d.pop_back();
	for(auto i : d){
		cout<<i<<" ";
	}
	queue<int> q;
	q.push(6);
	set<int> s;
	s.
	vector<int> v; 
	cout<<"\n";
}