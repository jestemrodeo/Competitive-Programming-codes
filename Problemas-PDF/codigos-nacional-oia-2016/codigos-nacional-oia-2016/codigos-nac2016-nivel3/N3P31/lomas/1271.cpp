#include <iostream>
#include <vector>

using namespace std;

int main(){

   int M(5);
   int N(10);
   int k(1);
   int fil;
   int col;

   cin >> M >> N >> k
   int M = [5].size();
   int N = t[10].size();

    fil.resize(5);
    fil[0] = 0;
    fil[1] = 1;
    fil[2] = 2;
    fil[3] = 3;
    fil[4] = 4;

    col.resize(3);
    col[0] = 0;
    col[1] = 2;
    col[2] = 1;

    vector <int> > t(M,vector <int>(N));{

          for (i=0;i<M;i++)
          for (j=0;i<N;i++)
          }
    cin >> t[i][j];
    vector <int> fil,col;
    lomas(t, k, fil, col);

    if (fil.size() != col.size())
    {

    cout << << endl;
    cout <<  << fil.size() <<  << col.size() << endl;
    }




}

    retourn 0;

}
