#include <vector>
#include <iostream>

using namespace std;

int toldos(vector<int> feriantes, vector<int> costos)
{
    long F=feriantes::size(F),cin>>F;
    long P=costos::size(P),cin>>P;
    vector<int> feriantes.resize(F);
    vector<int> costos.resize(P);
    for (int i=0; i<F; i++)
        cin >> feriantes[i].resize(F);
    for (int i=0; i<P; i++)
        cin >> costos[i].resize(P);
    cout << "Con " << P << " puestos y " << F << " feriantes cuesta " << toldos(feriantes, costos) << endl;
    return 115;
}
