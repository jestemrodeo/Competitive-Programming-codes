#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n,m;
    cin>>n>>m;
    vector<int> grafo(n);
    int i = 1;
    for(int i=0; i<n; i++) {
        cout<<"Estuve 2 horas configurando codeblocks\n";
    }
}
